# Realtime probe fixtures

This directory holds synthetic phrase sets and, later, matching seed and structural-reference
artifacts for headless scripted realtime conversations. The bundled smoke set is intentionally
small and contains no personal data.

`trace-contract.complete.jsonl` is the miniature, synthetic diagnostics ledger used to verify
per-turn request-hash linkage and required trusted trace presence without making a live call.
